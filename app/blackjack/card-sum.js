import cardValue from './card-value';

export default function (card1, card2) {
  if (cardName === king) {
    return 10;
  }
  if (cardName === queen) {
    return 10;
  }
  if (cardName === jack) {
    return 10;
  }
}
